return {
  install = { colorscheme = { 'vscode' } },
  -- Keep built-in syntax, filetype, compiler and remote-plugin support enabled.
  performance = { rtp = { disabled_plugins = { 'netrwPlugin' } } },
}
